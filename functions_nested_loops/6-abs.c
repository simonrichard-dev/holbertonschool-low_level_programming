#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@c : damnitsmine
 *
 * Return: 0 otherwise
 *
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	if (c < 0)
		return (-c);
	else
		return (0);
}
